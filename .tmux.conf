# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# window scroll
set -g mouse on

# status color
set -g default-terminal "screen-256color"

set -g status-bg "#242738"
set -g status-fg white
